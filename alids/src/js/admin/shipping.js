

/*
jQuery( function ( $ ) {

    (function () {
        var $this;

        return {
            init: function () {
                $this = this;
                $(document).on('request:done', function (e) {
                    if (e.obj == '#ads_shipping-form') {
                    }
                });

                return this;
            }
        };
    })().init();

});*/
